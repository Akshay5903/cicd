# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: setting first github successtoken
        run: |
          mkdir -p commitBranchCheckout
          cd commitBranchCheckout
          git clone https://github.com/Akshay5903/cicd.git
          cd cicd
          git checkout commitBranch
          git pull
          git config --global user.email "akshay.shar2019@gmail.com"
          git config --global user.name "Akshay 5903"
          git remote add origin https://Akshay5903:vimal@5903IBM@github.com/Akshay5903/project.git
          echo "19bcdeb8f93f9351cf88ae019704a587cb4c6419" > recentSuccessfulCommit.TXT
          git add --all
          git commit -m "Recent Commit Add"
          git push
        
